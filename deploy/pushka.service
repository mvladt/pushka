[Unit]
Description=Pushka (webpush scheduler)
After=network.target

[Service]
Type=simple
User=pushka
WorkingDirectory=/srv/pushka/current
ExecStart=/usr/bin/node src/main.ts
Restart=on-failure
RestartSec=3
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target
