# Задача: добавить ESLint и Prettier

## Проблема

Сейчас в проекте нет статической проверки стиля кода и форматтера. Единственная статическая проверка — `tsc --noEmit`, она ловит только типы.

При расширении проекта или работе с агентами легко наделать стилистических расхождений: разные кавычки, лишние импорты, неиспользуемые переменные, расхождения в форматировании.

## Желаемое поведение

- ESLint с правилами для TypeScript + Node 22 ESM, ловит распространённые ошибки и неиспользуемые сущности.
- Prettier — единый стиль форматирования (одинарные/двойные кавычки, длина строки, запятые).
- Команды `npm run lint` и `npm run format` (или `format:check`).
- Запускаются в CI как отдельный шаг.
- Опционально — pre-commit хук (через `simple-git-hooks` или `husky` — минималистичный вариант предпочтительнее).

## Принятые решения

_(заполнить при работе над задачей)_

- ESLint flat config (`eslint.config.js`) — современный формат, без устаревшего `.eslintrc`.
- Минимальный набор правил: `@eslint/js` recommended + `typescript-eslint` recommended. Без airbnb/standard — слишком жёсткие для пет-проекта.
- Prettier — дефолты + длина строки 100 (по аналогии с другими проектами).
- Без `eslint-plugin-prettier` — лучше `prettier --check` отдельным шагом, конфликтов меньше.

## Риски

1. **Шумные авто-фиксы по всему коду.** Митigation: один большой коммит «chore: apply eslint+prettier» с авто-фиксами, отделить от ручных правок.
2. **Расхождение с уже сложившимся стилем.** Митigation: пробежаться руками после автофикса, поправить кейсы где правило не имеет смысла.

## Когда делать

После того как CI/CD-задача (`cicd.task.md`) закрыта и стабилизировалась.
